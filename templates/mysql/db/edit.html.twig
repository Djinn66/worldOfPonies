{% extends 'base.html.twig' %}

{% block title %}Edit Db{% endblock %}

{% block body %}
    {% if is_granted('ROLE_USERADMIN') %}
        <h1>Edit Db</h1>

        {{ include('mysql/db/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('mysql_db_index') }}">back to list</a>

        {{ include('mysql/db/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
