<form method="post" action="{{ path('mysql_columns_priv_delete', {'host': columns_priv.host}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ columns_priv.host) }}">
    <button class="btn">Delete</button>
</form>
