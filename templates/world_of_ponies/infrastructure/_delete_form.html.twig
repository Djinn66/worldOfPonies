<form method="post" action="{{ path('world_of_ponies_infrastructure_delete', {'infrastructureId': infrastructure.infrastructureId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infrastructure.infrastructureId) }}">
    <button class="btn">Delete</button>
</form>
