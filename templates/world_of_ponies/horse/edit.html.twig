{% extends 'base.html.twig' %}

{% block title %}Edit Horse{% endblock %}

{% block body %}
    {% if (is_granted('ROLE_PROGRAMMER') or is_granted('ROLE_SPECIALIST') or is_granted('ROLE_SUPERUSER')) %}

        <h1>Edit Horse</h1>

        {{ include('world_of_ponies/horse/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('world_of_ponies_horse_index') }}">back to list</a>

        {% if not (is_granted('ROLE_SPECIALIST')) %}
            {{ include('world_of_ponies/horse/_delete_form.html.twig') }}
        {% endif %}

    {% endif %}
{% endblock %}
