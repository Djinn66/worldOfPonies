{% extends 'base.html.twig' %}

{% block title %}Edit Contest{% endblock %}

{% block body %}
    {% if (is_granted('ROLE_PROGRAMMER') or is_granted('ROLE_SPECIALIST')) %}
        <h1>Edit Contest</h1>

        {% if not is_granted('ROLE_NEWSREADER') %}
            {{ include('world_of_ponies/contest/_form.html.twig', {'button_label': 'Update'}) }}
        {% endif %}

        <a href="{{ path('world_of_ponies_contest_index') }}">back to list</a>

        {{ include('world_of_ponies/contest/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
