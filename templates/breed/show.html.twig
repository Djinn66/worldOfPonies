{% extends 'base.html.twig' %}

{% block title %}Breed{% endblock %}

{% block body %}
    <h1>Breed</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>BreedId</th>
                <td>{{ breed.breedId }}</td>
            </tr>
            <tr>
                <th>BreedName</th>
                <td>{{ breed.breedName }}</td>
            </tr>
            <tr>
                <th>BreedDescription</th>
                <td>{{ breed.breedDescription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('breed_index') }}">back to list</a>

    <a href="{{ path('breed_edit', {'breedId': breed.breedId}) }}">edit</a>

    {{ include('breed/_delete_form.html.twig') }}
{% endblock %}
